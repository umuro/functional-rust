// Example usage for 054-applicative-validation — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // validate_name(...);
    // validate_age(...);
    // validate_email(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
