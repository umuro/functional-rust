// Example usage for 005-reverse-list — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // reverse_inplace(...);
    // reverse_iter(...);
    // reverse_fold(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
