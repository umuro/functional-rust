// Example usage for 062-records — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // dev_config(...);
    // prod_config(...);
    // describe_config(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
