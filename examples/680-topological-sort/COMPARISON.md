# OCaml vs Rust: Topological Sort

## Kahn's Algorithm (BFS)
More naturally imperative - uses mutable in-degree array.

## DFS-based
More natural in OCaml - returns reversed post-order.

Both achieve O(V+E) complexity.
