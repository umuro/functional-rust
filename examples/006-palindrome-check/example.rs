// Example usage for 006-palindrome-check — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // is_palindrome_rev(...);
    // is_palindrome_iter(...);
    // is_palindrome_clean(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
