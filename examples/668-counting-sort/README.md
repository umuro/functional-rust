# Counting Sort

Non-comparison sort for integers in a known range.

## Complexity
- **Time**: O(n + k) where k is range size
- **Space**: O(k)
- **Stable**: Yes (with proper implementation)

## When to Use
- Small integer range
- Known bounds on values
- As a subroutine for radix sort
- When stability matters
