# OCaml vs Rust: proc macro derive

See example.rs and example.ml for side-by-side implementations.

## Key Points

1. Rust macros operate at compile time
2. OCaml uses ppx for similar metaprogramming
3. Both languages support powerful code generation
4. Rust's macro_rules! is built into the language
5. OCaml's approach requires external tooling
