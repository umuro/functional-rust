// Example usage for 003-pattern-matching — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // describe_pair(...);
    // area(...);
    // shape_name(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
