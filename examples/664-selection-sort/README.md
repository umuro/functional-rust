# Selection Sort

Repeatedly finds minimum element and places it at the beginning.

## Complexity
- **Time**: O(n²) all cases
- **Space**: O(1)
- **Stable**: No (can be made stable)

## When to Use
- When memory writes are expensive (fewest swaps)
- Simple implementation needed
- Small arrays
