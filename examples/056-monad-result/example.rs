// Example usage for 056-monad-result — demonstrates the library functions.
fn main() {
    // Import from the local library
    use example::*;

    // Example usage of functions:
    // parse_int(...);
    // safe_div(...);
    // compute_bind(...);

    println!("{} example ready!");
    println!("Run `cargo test` to see all tests.");
}
